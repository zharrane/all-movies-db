import React from "react"
import styled from "styled-components"

const Wrapper = styled.div`
  flex: 1;
  padding: 10px 20px;
`
const Content = styled.div``
const SideInforBar = () => {
  return <Wrapper> Dacito Dacito La la la la!</Wrapper>
}

export default SideInforBar
